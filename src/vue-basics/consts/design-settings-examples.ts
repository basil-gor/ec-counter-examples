import type { DesignSettings } from "@/vue-basics/types/design-settings";

export const DEFAULT_DESIGN_SETTINGS: DesignSettings = {
  buttonColor: "default",
  buttonSize: "medium",
  isImageHasBorder: false,
  imageSize: "small",
};
